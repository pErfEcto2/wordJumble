This repo was created just for fun. To install and start:

`sudo apt update -y && sudo apt upgrade -y`

`git clone https://github.com/pErfEcto2/wordJumble`

`cd wordJumble`

`make build`

`./wordJumble`
